# Member Dashboard and Certificate QA

Authenticated browser QA was completed for the user-authorised legacy member account. The consolidated dashboard displayed the member’s active status, annual membership plan, calculated valid-through date, assigned real MIS project, certificate access and non-sensitive account details in one protected view.

The certificate route rendered successfully and exposed only the authenticated member’s name, Membership ID, membership plan, joining date and calculated validity. It included a browser-native **Print / Save PDF** action and did not display the member’s identity-proof record, PAN data, password, address or any other member’s information.

The member had one active assignment at the time of QA. No additional project record or test membership data was created for this verification.

The public homepage was also checked after adding the persistent right-corner **Member Login** entry. A medium-width header conflict was corrected so the closed menu does not reserve blank vertical space; at constrained widths the primary navigation remains available through the menu control while the Member Login action remains visible in the header and within the expanded mobile navigation.

The authenticated member dashboard was rechecked against the server-owned member and assignment records. It displayed one active assignment, `PRJ-2026-001 · Digital capability`, and the annual membership validity date of `13/08/2027`. The new direct **Download PDF** action generated the expected member-specific certificate filename in the browser download history. The generated file includes only certificate-safe membership data and is accessed from a member-authenticated dashboard.

With the same authenticated member session, the public header rendered the member’s initials and first name instead of the generic Member Login prompt. The profile affordance pointed to the member’s own dashboard, and the responsive navigation exposed the same **My member account** entry with the initials badge.

Mobile captures confirmed that the public entry remains responsive and that signed-out access to the member dashboard and certificate route stops at the intentional member-login gate. The authenticated desktop check verified the corresponding personal-data views and download controls.

The owner-provided official AASW certificate template was integrated as the protected certificate preview and direct-PDF background. Authenticated visual QA confirmed that the legacy member’s name, Membership ID, annual term `(1 Year)` and issue date rendered in the approved template locations while the fixed Foundation branding, recognition text and signatories remained intact. The official-template PDF download completed successfully in browser history.

The generated PDF was inspected visually against the same official template. A source-date masking adjustment was applied so the dynamic issue date is the only date shown in the downloadable document.

Final authenticated preview QA confirmed the corrected official layout with only the current dynamic issue date visible, alongside the member name, Membership ID and annual term placement.

The corrected template-based PDF was downloaded successfully as a new member-specific file through the authenticated certificate view.

After the development-server restart, the authenticated member dashboard first displayed the new accessible loading animation and then resolved to the member’s own membership data and one assigned project. The live dashboard showed Membership ID `AASW-2026-H1COR`, annual validity through `13/08/2027`, and the assigned `PRJ-2026-001 · Digital capability` record. The preview application route itself responded successfully in the verification session.

The restarted certificate route also resolved to the official AASW template after its managed image asset loaded. It rendered the authenticated member name `Aviral Trivedi`, Membership ID `AASW-2026-H1COR`, annual term `(1 Year)` and member issue date in the approved positions.

Before the requested frontend-only dashboard polish, the authenticated desktop dashboard rendered the current member KPI row, one assigned project, membership certificate controls and the password panel. This baseline was captured for subsequent before/after comparison; no API, routing, database or displayed member-data changes are in scope for the styling work.

After the frontend-only dashboard polish, authenticated desktop QA confirmed four equal-width KPI cards with individual left accents, a compact welcome section with divider, equal top-aligned Project and Change Password panels, styled project status treatment, a full-width green certificate panel, and the membership-detail grid. All original member, project and expiry values remained unchanged.

The authenticated browser session could not programmatically resize its fixed viewport for a second live mobile capture. The mobile breakpoint was nevertheless implemented in the scoped dashboard stylesheet at `768px`: KPI cards become a 2×2 grid, project/password panels stack, certificate actions become full-width vertical controls, membership details become single-column, and the header certificate label reduces to its icon while preserving the destination.

Authenticated sidebar-dashboard QA confirmed that the default **My profile** tab renders the member initials, current active status, real name, Membership ID, role, email, available location and existing assignment count. The profile update request remains a Foundation email action and photo upload shows a transparent coming-soon notice because the existing protected member API does not expose profile-edit or avatar-upload procedures.

The **My membership** tab preserved the active plan, validity dates, progress treatment and official certificate download/view actions. The **My services** tab displayed the real assigned-project count and certificate action alongside clearly labelled coming-soon tiles for unsupported documents, notifications and events; no nonexistent API or route was introduced.

The **Change password** tab retained the existing protected password mutation behind current/new/confirmation inputs, show/hide controls, client-side strength guidance and mismatch/error treatment. The active sidebar section persisted as `member_dashboard_active_section=password` in the authenticated browser’s local storage after selecting the password tab.

After a full authenticated dashboard reload, **Change password** was restored as the selected sidebar section, confirming client-side active-section persistence without altering routing or backend account logic.

Authenticated QA of the enhanced sidebar confirmed that the new **Home** section renders AASW Foundation information, links to the approved About and Programmes routes, and offers the Foundation support contact. The desktop header also exposes an accessible labelled control to collapse or expand the sidebar. Existing membership, services and password tabs remained available.

With the sidebar collapsed, the navigation became a compact icon rail while the labelled tooltip affordances remained available. The **My profile** tab still rendered its authenticated member details and exposed the now-enabled upload entry, restricted in the browser to JPG, PNG and WebP image selection before the protected server-side validation runs.

On 15/08/2026, the user-authorized Aviral Trivedi profile photograph was attached only to Membership ID `AASW-2026-H1COR` in managed member-profile storage. A fresh authenticated dashboard load confirmed that the sidebar avatar renders the uploaded image for this member profile; the image is not included in public website content.

The top-header sidebar control was removed. Authenticated interaction confirmed that clicking the upper member-sidebar identity area alternates the desktop sidebar between the compact icon rail and the expanded identity/navigation state; the control exposes matching accessible labels for expansion and collapse while keeping member navigation buttons independent.

On 15/08/2026, the refreshed authenticated **Home** view was verified for the authorised member account. The page presents the PDF-approved Aapka Apna Social Welfare Foundation framing, the approved “Fueling women’s success through tech & enterprise” message, programme pillars for digital skills, green entrepreneurship, mentorship, shared learning and community building, and the supported 800+, 300+ and 30+ impact figures. The screenshot review confirmed the intended warm editorial hierarchy: a forest-green mast, marigold primary action, purpose statement, individually coloured programme cards, neutral impact rail and dark participation callout. Existing protected membership, sidebar, certificate, profile-photo and password interactions were not changed.

The responsive treatment is covered in the scoped member-dashboard stylesheet and presentation regression suite: the five programme pillars resolve from five columns on wide screens to three columns on tablet and two columns on mobile; the mast, purpose, impact and participation sections stack without horizontal overflow at the mobile breakpoint. The visual-capture utility intentionally lacks the authenticated member cookie, so its capture correctly stopped at the member-login gate; the authenticated browser session was used for the actual Home-content and design verification.

On 15/08/2026, both Member Home editorial calls to action were verified in the authenticated member session as dedicated in-dashboard destinations. **Discover AASW** now opens `/member/discover` within the preserved member sidebar and presents the approved Foundation purpose, objective, work approach and governance framing. The internal **See the programmes** action then successfully opened `/member/programmes`, which rendered the five approved programme areas with distinct editorial card treatments and a return-to-Home path. Neither route exposes member data beyond the existing authenticated shell, and the earlier public `/about` and `/programs` links are no longer used by these two Member Home actions.

The same authenticated `/member/programmes` view was rechecked after interaction polish. The route retains the existing member loading state while identity data resolves, then applies brief staged content reveals without blocking reading or navigation. Programme cards, icons and primary actions use short hover/focus transitions, while the stylesheet removes those non-essential animations for visitors who request reduced motion. The member shell, account data, sign-out, certificate entry and internal navigation remained intact during this verification.

On 16/08/2026, membership completion was extended so new applications are stored with an **approved** application status while their separately protected member account remains active. The Foundation Gmail welcome message now includes the allocated Membership ID, a one-time 72-hour password-setup action and a distinct 72-hour certificate-view action. The certificate link is backed by a database-stored hash, replaces any prior unused email-certificate link for the same member, and exposes only the eligible member’s certificate data while valid. The existing Member Portal certificate remains the permanent authenticated access route after password setup. Automated verification covered no-plaintext-password email content, hashed/expiring token persistence, automatic approval state, route registration and invalid-link fallback; no test email was delivered to a real applicant.

On 16/08/2026, the Member Portal login-success route was enhanced with a one-time welcome transition. A successful member login writes an ephemeral session flag before navigating to the protected dashboard; the dashboard displays the official AASW logo, **Welcome to AASW Foundation** message, paper-plane movement and a rightward paper-style reveal, then clears the flag before showing the existing protected member shell. The available authorised member session was used to confirm that the protected dashboard still loads with the correct member identity, navigation and Home presentation after the intro lifecycle. Browser automation cannot reliably capture the short transition frame; source-level coverage verifies the one-time flag, official copy, reveal hooks and reduced-motion path. Users who request reduced motion bypass the animated delay.

On 16/08/2026, the same post-login motion sequence was upgraded with lightweight animated aurora, orbit and texture layers in the welcome background, a longer paper-plane flight with trailing detail, and an interstitial paper wipe. Once the plane sequence completes, the protected dashboard header, sidebar rail and content are revealed in a short staggered fade-and-rise sequence. Motion is limited to opacity and transform; the full intro, background layers and dashboard reveal animations are disabled for reduced-motion users. Automated coverage verifies the new background hooks, plane timing hooks, dashboard-reveal state and reduced-motion safety. Existing authorised dashboard rendering remained available after the animation lifecycle.
