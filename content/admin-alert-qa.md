# Foundation Admin Alert QA Notes

On 16/08/2026, the refreshed `/foundation-admin` route was opened after the auto-approved-member alert feature and server restart. The Foundation-branded sign-in gate rendered successfully, with the authorised-owner login action visible and no public records exposed. The new alert panel itself remains protected behind the same owner-only session; it will be verified with live member data after the Foundation owner signs in. Automated API and presentation coverage validates the alert list/read surface, while no real membership applicant or email delivery was used for testing.

The authenticated Foundation workspace was subsequently captured in the available admin session. The new **Member approvals** panel rendered above the summary cards, correctly showed the zero-alert empty state, and the new **Unread approval alerts** KPI rendered with a value of zero. Existing membership, contact, donation, payment, field-photo and Drive panels all continued to load in the same workspace. The current historic membership record was intentionally not backfilled into the new alert table; only future successful auto-approvals create alert records.
